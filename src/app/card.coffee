
# class Card extends Controller
#     constructor: ($scope, someService, anotherService) ->
#         $scope.coolMethod = someService.coolMethod()

# class another extends Service
#     constructor: (stuff) ->
#         stuff.things = (lal) ->
#             'dddthings'
